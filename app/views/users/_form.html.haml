= semantic_form_for @user do |form|
  = render 'shared/error_messages', :object => form.object
  = form.inputs do
    = form.input :name
    = form.input :email
    = form.input :password
    = form.input :password_confirmation, :label => "Confirmation"
  = form.buttons do
    = form.commit_button @title